{
  "name": "synthia-claw",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @synthia/shared && npm run build --workspace @synthia/server && npm run build --workspace @synthia/web",
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev --workspace @synthia/server\" \"npm run dev --workspace @synthia/web\"",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=22"
  }
}
